# API Reference

Base URL: `http://127.0.0.1:8000`

## `GET /health`

Health check used by the extension popup to confirm the backend is reachable.

**Response**

```json
{ "status": "ok" }
```

---

More endpoints (extraction, export) will be documented here as they're added.
